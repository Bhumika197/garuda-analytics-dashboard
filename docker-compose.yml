version: '3.8'

services:
  web:
    build: .
    command: python backend/app_dev.py
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - PORT=5000
    volumes:
      - ./backend:/app
